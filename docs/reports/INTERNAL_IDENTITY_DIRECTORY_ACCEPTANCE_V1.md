# Internal Identity Directory Authority acceptance

Repository: mayf3/auth-service. Recorded at 2026-09-06 (batch exact-head acceptance).

Owner dispatch (WORKFLOW_ASSIGNEE_CANONICAL_IDENTITY_RECONCILIATION_V1, CONTINUE_SAME_GOAL) recorded `OWNER_DECISION = BATCH_EXACT_HEAD_ACCEPTANCE = YES` accepting the exact reviewed head `f2b7d4c91ad657464816108062145ae95c21d686` as the authority `AUTH_SERVICE_INTERNAL_IDENTITY_DIRECTORY_V1`, alongside batch siblings dsh `7f620ad87ebadc9571eaf527bc370f259dfd9cc6` (AGENT_CORE_INTERNAL_AGENT_DIRECTORY_V1) and svc-workflow `893d36f76702c307ee220dead717f64460b05f49` (SVC_WORKFLOW_CANONICAL_IDENTITY_RECONCILIATION_V2).

Basis: FINAL_HEAD_MECHANICAL_VERIFICATION by a fresh independent read-only verifier returned `FINAL_HEAD_VERDICT = ACCEPT`, `FINAL_HEAD_SEMANTIC_DRIFT = NONE`, `BLOCKER_UNION = []` (all seven checks PASS: bounded post-re-audit commit scope, declared V1→V2 successor delta only, frozen clauses verbatim, external revision binding, three-authority baseline-policy consistency, no retired-object authorization). The earlier semantic audit chain (ACCEPT_WITH_BLOCKERS → one bounded repair → fresh re-audit 0 SHIP_BLOCKERS → prescribed residual closure) is recorded in the branch history.

Accepted semantics: canonical identity-directory lookup is a BASIC authenticated internal read capability for all ACTIVE authenticated canonical internal AGENT and SERVICE Principals (baseline entitlement `identity-directory`/`auth.directory.read` and `agent-directory`/`agent.directory.read` through the existing provisioning machinery, no per-consumer lookup identities); minimum data `{principalId, agentId, principalStatus}` / `{agentId, exists, enabled}`; the dedicated Workflow admission model (principal cedb954a-3d99-4e5a-b568-d312441bcc56, client svc-workflow-canonical-admission-v1, audiences workflow-principal-admission / workflow-agent-admission) is retired and MUST NOT be provisioned or reintroduced (it never was).

This transaction is lifecycle-only: successor frontmatter flips to accepted with the exact accepted head; predecessor AUTH_SERVICE_WORKFLOW_CANONICAL_ADMISSION_V1 flips to superseded with superseded_by this authority. Zero normative semantic change; authoring-time readiness wording inside the frozen reviewed bodies records its authoring-time state (frontmatter owns lifecycle). No production mutation occurred in this transaction.
